/*
#include <stdio.h>

int main(void)
{
    int a1, a2, a3, a4, lar, small;

    printf("\nEnter four integers: ");
    scanf_s("%d %d %d %d", &a1, &a2, &a3, &a4);

    lar = small = a1;
    if (lar < a2)
        lar = a2;
    if (lar < a3)
        lar = a3;
    if (lar < a4)
        lar = a4;
    if (small > a2)
        small = a2;
    if (small > a3)
        small = a3;
    if (small > a4)
        small = a4;

    printf("Largest: %d\nSmallest: %d\n\n", lar, small);
    return 0;
}
*/